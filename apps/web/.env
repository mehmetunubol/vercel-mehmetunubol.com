# Committed non-secret default so local production builds match the deployed site.
# Override per-environment in Vercel if needed. Secrets must go in .env*.local (gitignored).
NEXT_PUBLIC_SITE_URL="https://mehmetunubol.com"
